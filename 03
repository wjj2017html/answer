vue 中key的作用和工作原理


1.最常见 的v-for 中要用到key属性，它是为了让item有一个唯一的识别身份，下次如果这个v-for对应的数据有变化，会先去找对应KEY值，看看是否变化，没有变化采用‘就地复用’原则，有变化采用diff算法，这样可以高效的更新虚拟DOM

2.在创建VUE实例，对data数据进行 watch 时 会创建一个KEY，这个Key是进行依赖收集， 更新vNode时，需要从旧vNode列表中查找与新vNode节点相同的vNode进行更新，如果这个过程设置了属性key，过程就会快很多
